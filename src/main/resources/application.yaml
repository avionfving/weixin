# 配置MySQL数据库的连接参数
spring.datasource:
   url: jdbc:mysql://127.0.0.1:3306/zdf?useSSL=false&serverTimezone=CST
   username: zdf
   password: 1234

#修改运行时的端口号，因为一个人只能被一个人使用。
#如果多个人使用一台服务器，自然使用不同的端口。
server.port:8188;
